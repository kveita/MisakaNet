# Handoff — 2026-09-06（DCO 体验修复轮，issue #1498）

> 合并：`8cc2967f3`（PR #1502，squash）→ main 现 HEAD `8cc2967f3`。
> 上游承接：`docs/maintainer/handoff-2026-09-05-audit-improvements.md`（Wave 3 及其挂起项）。
> 本会话主题：取证并修复贡献者抱怨的 DCO 体验问题（issue #1498）。

---

## 1. 会话概览

- **触发**：issue #1498（hummern，PR #1487 作者）：`needs-dco` 标签不自动清除，
  补签 force-push 后 bot 评论仍红，被迫推新提交触发重扫。
- **动作**：REST 取证（PR 时间线/check-runs/标签）+ 全仓代码定位根因 + 量化影响 →
  P0 backfill 清标签 → P1 workflow 修复（PR #1502）→ P2 文档 → 回复并关闭 issue。
- **成果**：102 个失实 `needs-dco` 标签清除（3 open 解锁 + 99 closed 卫生）；
  标签生命周期改为"每次 push 重扫、全签自动摘除"；3 个 open PR（#1400/#1411/#1461）
  不再被误判为"等作者补签"。

## 2. 根因（详见 docs/maintainer/dco-experience-issue-1498.md）

1. `needs-dco` 标签**只加不删**：`pr-shape-guard.yml` Gate 4 仅在未签时 add，
   全仓无任何 `removeLabel('needs-dco')`；`pr-quality-gate.yml` 反把残留标签当
   `ready-to-merge` 硬阻断 → 修好后永久红标。
2. 三套 DCO 实现语义分裂（dco-check 含 merge 提交 / dco-audit `--no-merges` /
   shape-guard 含 merge 提交）→ 误判源。
3. 失败评论只追加不清扫（REGULATORY BLOCK + 逐次 audit 报告）→ 界面自相矛盾。

## 3. 量化证据

- merged 415 中 64 (15.4%) 仍挂 `needs-dco`；open 3 个全部 0 未签名提交（100% 误挂）。
- #1487：13:26 labeled → 16:47 merged，全程无 unlabeled；最终 head 上 dco/audit/quality-labels 全绿。

## 4. 改动（PR #1502，merged `8cc2967f3`）

| 文件 | 改动 |
|---|---|
| `.github/workflows/pr-shape-guard.yml` | Gate 4：全签 → `toRemove needs-dco`（每次 push 生效）；merge 提交豁免 |
| `.github/workflows/pr-quality-gate.yml` | `ready-to-merge` 由 DCO check run 结论驱动；标签过清/败加双保险 |
| `.github/workflows/dco-check.yml` | `--no-merges` 对齐 |
| `.github/workflows/pr-checks.yml` | audit 报告 marker upsert（就地更新）；DCO 过清 REGULATORY BLOCK |
| `CONTRIBUTING.md` | DCO 自动清除说明 + Node.js Test Conventions（fixtures/、`this.skip()`） |
| `docs/label-system.md` | `needs-dco` 自动清除 |
| `scripts/dco_label_backfill.py` | 维护者标签回填工具（本次已用） |
| `docs/maintainer/dco-experience-issue-1498.md` | 取证+影响+方案+执行记录 |

## 5. 状态与验证

- backfill：`removed=102`（3 open + 99 closed）、`kept=200`（确有未签名提交，语义真实）。
- #1502 CI：DCO/audit/audit-shape/tests 矩阵/CodeQL 全绿（pr-agent 为非阻塞 review bot）。
- audit 报告 marker upsert 首跑实测生效（#1502 评论区单条就地更新）。
- issue #1498：已回复（致谢+根因+修复+backfill 数据）并 closed。

## 6. 运维备注

- **日常**：`python3 scripts/dco_label_backfill.py --dry-run` 可复查是否有失实标签
  （--dry-run 只报告不删）。
- **行为变更提示**：merge 提交不再要求 sign-off（三处检查已统一）——对"git merge main"
  的贡献者是减负；对旧 PR 语义放宽，已在文档注明。
- 上一会话挂起项不变：audit 红门 PR 队列、DSH 上游提案、MCPVault 认领、
  registry 发布（mcp-publisher）、wrangler KV 占位符等（见 handoff-2026-09-05-audit-improvements §Wave 3）。

## 7. 回滚

- 整体：`git revert 8cc2967f3`。标签自动摘除逻辑在 shape-guard/quality-gate 两处，
  若需回退旧行为需同时 revert 该提交（含 dco-check --no-merges 与 pr-checks upsert）。

---

# Wave 2 更新（同日，bot 文案一致性 + #1430 收尾）

## 评估结论：md 之外的"机器人回复文案"此前未更新 → 已补

初轮修复只覆盖了标签状态机/检查语义/文档（md）。审计所有 bot 对外文案后发现
**仍与旧行为矛盾**，已随 **PR #1503**（merged `34041298b`）修复：
- `pr-welcome.yml`：对**所有**首 PR 无条件发 "Fix DCO First"（即使已全签，如 #1487/#1430），
  结尾还写 "CI runs automatically once DCO passes" → 改为**按 commits 实际状态条件展示**，
  通过则显示 "DCO: all commits signed ✅"；措辞承诺自动清除、勿推空提交。
- `dco-check.yml`：检查输出补修复命令 + merge 豁免 + 自动清除说明。
- `.github/actions/dco-audit/action.yml`：废弃惊悚的 "REGULATORY BLOCK / AUTO-MERGE: PINNED"
  文案 → 友好修复指引（含 `/fix-dco` 提示 + 自动清除说明）；评论改 **marker upsert 不堆叠**。
- `pr-checks.yml`：改用**本地** action（外部 `Ikalus1988/dco-audit@v1` 已 2 个月未更新，
  且本地副本曾是死代码——单一事实源问题一并解决）；清理同时匹配新 marker 与旧文案。

## audit 管线健壮性（#1430 排查中发现的真问题，随 #1503 修复）

- **Dependency Audit 在 workflow_dispatch 下必失败**：无 PR base/head SHA → `git diff` 报错；
  且 always() 报告因此对 docs-only PR 打出误导性 "❌ Test suite failed"（实测复现）。
  → dispatch 时跳过 dep audit；Test Suite 报告/硬门禁把空 OUTCOME（manual 跳过）当 "未失败"。

## PR #1430 卡点诊断与处置（docs: MCP intake 引导，#1071）

- **卡点**：内容早已就绪（09-02 维护者要求的格式修复 + 09-03 rebase 均完成），
  但 `audit` 检查红 —— 拉日志定位：`tests/test_intake_kind.py` 2 例因
  `ModuleNotFoundError: mcp.server.fastmcp` 失败 = **mcp 2.x 环境漂移**（旧
  `mcp_http_server.py` 用 v1 导入），与 PR 内容无关；上游 #1478（mcp SDK v2 迁移）已修，
  但 #1430 的 merge ref 停在 09-03 main（#1478 之前）→ 陈旧红标。
- **处置**：删掉误发的手动 audit 报告评论 → 说明根因并 **squash 合并（`cab150ff3`）**。
- **教训**：fork docs PR 若长时间未 rebase，其 CI 反映的是旧 base 状态——维护者应按
  "merge ref 是否已过期"判读 audit 红，而非等作者；另全量测试对 docs-only PR 仍会跑
  （scope 判定不含 docs-only 分支），是 audit 噪音来源之一（后续可优化 scope 规则）。

## 状态速览

- main HEAD `34041298b`（含 #1430 + #1503）。PR #1503 已合并。
- 上一节挂起项不变；新增待办：可复查 `scripts/dco_label_backfill.py --dry-run`
  确认无新失实标签。

---

# Wave 3 更新（同日，zsxh 反馈批量处置：#1400 CI 不触发 / audit / gate）

zsxh 反馈三类"无法修复（需维护者）"问题，逐一定位并处置：

## 1. #1400 CI 不触发（cross-platform 未跑）——根因 = merge 冲突，非 repo 配置
- 证据：`merge_commit_sha=None`、`mergeable=False`（dirty）；head `61e06d26` 的
  **pull_request 工作流 run 数为 0**，只有 pull_request_target（shape guard/welcome）在跑。
  → GitHub 无法为冲突 PR 生成 merge ref，pull_request 事件工作流不启动。
- 附带：该分支 diff 是巨型噪音（删 codeql.yml/deploy-worker.yml、data/lessons.json −6567、
  docs/mcp-intake-guide.md −223 等 = 旧 main 误合并/revert 残留），无法干净合并。
- 处置（作者侧，维护者可代推）：从当前 main 重建，仅保留 e2e 测试增量。

## 2. audit 红（#1494/#1493/#1461/#1452/#1451/#1437/#1399）——主仓库已修，多数为陈旧 run
- **mcp v2**：主仓库已由 #1478（09-05 03:51 合并）修复；本地全仓已无 `fastmcp` 导入。
- **版本漂移**：#1494/#1493 的 audit 红（09-05 16:21）= `test_version_consistency.py::test_pypi_line_lockstep`
  瞬时失败（pyproject 2.28.0 vs server.json pypi 2.27.1），#1492（registry/pypi 对齐 2.28.0）已修复；
  本地验证 test 通过。
- **实证**：close/reopen 触发新 run 后 #1399、#1437 的 **audit 均 ✅**；#1494/#1493 的 reopen
  被 GitHub 按 head sha 去重（只触发 pull_request_target，未触发 pull_request），下个 push 即绿。

## 3. gate 失败（lesson 重复标题/近似）——真实内容信号
- **#1452（WebCrypto, #1398）/ #1451（Vitest, #1384）= 重复提交**：lesson 已于 08-30/09-01
  由 zsxh 本人合入 main（90585ef30/96981af92，同一 issue）；gate 报 duplicate title +
  100% similar → **已按重复关闭**。
- **#1437/#1399（provenance 批量）**：#1399 报 256× missing evidence_level + 少量
  duplicate title/near-dup——批量补 provenance 元数据时未给部分旧 lesson 补 evidence_level；
  需作者补齐 frontmatter 或排除镜像。
- **#1461**：audit 已绿；shape-risk = 标题 docs: 但含 scripts 改动（范围越权）→ 改标题/拆 PR。

## 4. 待办
- #1411（Provenance batch 2）当前**全绿**（audit/gate/dco/tests 全 success），仅剩
  risk:high/generated-file human review → 优先审。
- #1494/#1493：作者下个 push 后 audit 应绿（gate/dco/tests 已绿）→ 可合（lessons 需人工）。
- #1400：等作者重建分支（或按需用 zsxh PAT 代推重建）。

---
*Wave 3 附记于 2026-09-06。*

---

# Wave 4 更新（同日，zsxh PR 批量合并轮）

zsxh 完成四 PR rebase/瘦身后（详见其上一条汇总表），维护者侧逐项处置：

## 已合并（3）
| PR | 说明 | merge |
|---|---|---|
| #1437 | 56 devops lessons provenance + evidence_level: E1（纯 frontmatter）| `24f3c062e4` |
| #1461 | MCP 远端端点评估报告（docs/mcp-eval-20260903.md）+ `import sys` 修复；docs-only 标签移除、重命名 docs+fix: 后 shape-guard 复跑 shape-safe | `be252c78fb` |
| #1411 | **299 lessons provenance batch + arch-review 三件套**（瘦身 +518/−75，DCO 补签后标签自动清除——#1498 流程实证生效）| `22842a48e6` |

## 已关闭（3）
- **#1452/#1451**：与 zsxh 本人已合入 main 的 lesson（#1398/#1384，commit 96981af92/90585ef30）100% 重复 → duplicate closed。
- **#1399**：与 #1411 299 个 lesson **完全重叠**（#1411 多 arch-review 且已修 evidence_level）→ superseded by #1411 closed。

## 关键发现与决策
1. **#1400 与 #1411 曾高度重叠**（lesson-security.yml +29/−24 逐字节相同、data/*.json 大 churn 同源）——#1411 瘦身后已分离；#1400 仍含 **lesson_gate.py 策略放宽**（`provenance.evidence` 可替代 `evidence_level` + 跨目录重复标题豁免）与 lesson-security.yml 重写 → **语义存疑**（provenance.evidence 是"入库溯源"，非 E0-E4 证据等级），**未合并，待拍板**。
2. **#1411 gate 256× missing evidence_level = 预存 legacy 债务**（main 上本就缺，gate 只校验被触碰文件才暴露）。已建 **issue #1506** 跟踪：legacy evidence_level 补齐 + gate"新增 vs 元数据触碰"差异化校验 + en/ 镜像 dup 误报。
3. **quality-labels 403**（fork PR 偶发）：pr-quality-gate `addLabels` 无 try/catch 的既有噪音，非本会话引入；可后续补防护。
4. **gate 校验范围**：lesson-gate 按"PR 触碰文件"全量新标准校验 → provenance 类元数据批量 PR 必然撞历史债。长期修复 = 差异化校验（见 #1506）。

## 待办
- **#1400**：需拍板 ——（a）拆出 e2e 测试单独合、（b）接受其 gate/security 策略变更（建议否，语义存疑）、（c）暂缓。
- #1494/#1493：audit 陈旧红（09-05 版本漂移，已修）→ 下个 push 即绿；lessons 需人工合并。
- 网络：github.com git 协议 16:40 起间歇不可达（REST 正常）；本 Wave 附记待网络恢复后推送。

---
*Wave 4 附记于 2026-09-06。*

# Wave 4b（同日收尾）

- **#1400 合并**（`2574167a17`）：zsxh 二次瘦身后仅剩 `tests/e2e_mcp_pipeline.py`（+180 独立 E2E，覆盖 #1359-1361）——policy 改动全部拆出，全绿合并。
- **#1505**（zsxh 新开 "discuss: gate evidence_level relaxation + security scan rewrite"）：策略内容独立成 PR，已发维护者评审——
  - fanuc tags −2：接受
  - lesson-security.yml 重写：接受（建议补 `~~~` fenced 支持）
  - lesson_gate.py：**evidence_level ⇄ provenance.evidence 等价建议否决**（语义不同维；新 lesson 不能无 E 级过门禁）；跨目录 dup 豁免建议收窄为 en/ 或同 stem 镜像 → 指向 #1506 差异化校验
- 本地 handoff 已 rebase 推送 `ac9714152`。

---
*Wave 4b 附记于 2026-09-06。*

# Wave 5（同日收尾：zsxh 反馈全部清零）

zsxh 三轮反馈（#1400 CI 不触发 / audit / gate / quality-labels 403）至此全部处置完毕，主仓库侧基础设施修复 3 项 + 贡献 PR 合并 6 个：

| PR | 内容 | merge |
|---|---|---|
| #1400 | e2e MCP pipeline 测试（瘦身纯测试）| `2574167a17` |
| #1411 | 299 lessons provenance + arch-review（#1399 superseded）| `22842a48e6` |
| #1461 | MCP 评估报告 + import 修复 | `be252c78fb` |
| #1437 | 56 lessons provenance + evidence_level | `24f3c062e4` |
| **#1507** | lesson_gate 接受 provenance.evidence（zsxh 拆出的 gate 修复）| `edd44ae7a6` |
| **#1508** | quality-gate label 写入 best-effort（fork 403 修复，回应 zsxh CI 配置问题）| `179c368a90` |
| #1505 | fanuc tags + security 扫描重写 + near-dup error→warning | `4debc9b9fd` |

**策略修正记录**：此前评审曾建议否决"provenance.evidence 顶替 evidence_level"；基于语料现实修正——main 上 336/360 lessons 无 E 级（E0-E4 从未回填），#1411 已确立 provenance.evidence 为 legacy 证据机制 → 等价接受（#1507）。新 lesson 仍要求 E 级 + dup 豁免收窄等严格化保留在 **#1506**。
**遗留**：#1506（legacy evidence_level 差异化校验/en 镜像 dup）、#1494/#1493（audit 陈旧红，下个 push 即绿）、handoff 本文档随会话推进已多次追加（Wave 2-5）。

---
*Wave 5 附记于 2026-09-06。*
